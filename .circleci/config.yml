version: 2
jobs:
  build:
    branches:
      only:
      - master
      - develop
      - addinCircleCI
    docker:
    - image: circleci/openjdk:8-jdk-browsers
    working_directory: ~/java-tron
    steps:
    - checkout
    - run:
        name: Daily Build Report
        command: sh runcontinue.sh


    - run:
        name: Download Links
        command: sh DownloadLinks.sh