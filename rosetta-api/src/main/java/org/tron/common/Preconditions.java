package org.tron.common;

import org.tron.model.NetworkIdentifier;

public class Preconditions {

  public boolean checkNetworkIdentifier(NetworkIdentifier networkIdentifier) {
    return true;
  }
}
