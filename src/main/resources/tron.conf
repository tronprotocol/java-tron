# levelDB
leveldb {
  directory = "tron-data"
  utxodir = "tron-utxo-data"

}

# database  directory
database {
  directory = "database"
}

# database directory for test
database-test {
  directory = "database-test"
}

# Kafka
kafka {
    host = "127.0.0.1"
    port = ":9092"
}